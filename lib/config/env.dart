class Env {
  // TODO: For production, inject these via --dart-define or use a secure vault.
  static const String geminiApiKey = 'gemini_api';
  
  // Add other keys here
  static const String googleMapsApiKey = ''; 
}
